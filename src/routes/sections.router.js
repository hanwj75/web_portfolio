import express from "express";

const router = express.Router();

/**
 * @desc 섹션 추가 API
 */

/**
 * @desc 섹션 내용 수정 API
 */

/**
 * @desc 섹션 순서 일괄 변경 API
 */

/**
 * @desc 섹션 삭제 API
 */

export default router;
